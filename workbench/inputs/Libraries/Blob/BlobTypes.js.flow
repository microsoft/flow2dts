opaque type BlobCollector = {...};

type BlobData = {
  blobId: string,
  offset: number,
  size: number,
  name?: string,
  type?: string,
  lastModified?: number,
  __collector?: ?BlobCollector,
  ...,
};

type BlobOptions = {type: string, lastModified: number, ...};
export type {BlobCollector};

export type {BlobData};

export type {BlobOptions};
