// @flow
declare var AnimatedImplementation: typeof $1;
const $1 = require("./AnimatedImplementation");

declare module.exports: {|
  ...typeof AnimatedImplementation,
  // $FlowFixMe createAnimatedComponent expects to receive types. Plain intrinsic components can't be typed like this
  div: $FlowFixMe,
  // $FlowFixMe createAnimatedComponent expects to receive types. Plain intrinsic components can't be typed like this
  span: $FlowFixMe,
  // $FlowFixMe createAnimatedComponent expects to receive types. Plain intrinsic components can't be typed like this
  img: $FlowFixMe,
|};
