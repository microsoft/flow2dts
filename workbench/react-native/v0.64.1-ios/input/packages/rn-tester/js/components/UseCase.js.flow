// @flow
import * as React from "react";

type Props = $ReadOnly<
  {|
    children?: React.Node,
    title?: ?string,
    note?: ?string,
    ios?: ?boolean,
    android?: ?boolean,
  |},
>;

declare function UseCase(props: Props): React.Node;
export {UseCase as default};
